##### chatagne 自定义模块

用于控制 OSCVideoPlayer

支持组播/单播

     组播地址默认`239.0.0.139:8000`, 组播地址运行时可切换, 组播端口暂时不支持切换
    
     本机监听端口:`12000`, 运行时可切换 

支持多节点自动同步播放 

如果不希望节点加入组播, 在 `Multicasr Members` 容器中使它 `Leave`

所有命令均有提示